name: gmao_mobile
description: App móvil GMAO para técnicos de campo (GRUPO EBIM)
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.5.6
  image_picker: ^1.1.2
  mobile_scanner: ^5.2.3
  firebase_core: ^3.6.0
  firebase_messaging: ^15.1.3
  connectivity_plus: ^6.0.5
  shared_preferences: ^2.3.2
  intl: ^0.19.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
